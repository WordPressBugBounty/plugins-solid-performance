/**
 * Selector function to get the entire state.
 *
 * @param {PreloadState} state - The current preloading state.
 *
 * @returns {PreloadState} The entire state object.
 */
export const getPreloadStatus = ( state ) => {
	return state;
};
